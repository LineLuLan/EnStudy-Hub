export default function ReviewPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Ôn tập</h1>
      <p className="mt-2 text-sm text-zinc-500">
        TODO Tuần 3: flashcard + rating buttons + FSRS scheduler + Zustand session store.
      </p>
    </div>
  );
}
